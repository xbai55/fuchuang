# Project-wide Python dependencies
# Install from repository root:
#   pip install -r requirements.txt
# Backend dependency entry uses the same source via backend/requirements.txt -> ../requirements.txt
# Frontend dependencies are managed in frontend/package.json + frontend/package-lock.json
# OCR 依赖建议默认使用 CPU Paddle，避免不同 CUDA 运行时下的额外冲突。
# 如需 GPU Paddle，可手动替换为 paddlepaddle-gpu==2.6.2 并安装 cuDNN 8.9：
#   conda install -n fuchuang -c conda-forge cudnn=8.9

# GPU wheels source (PyTorch CUDA 12.4)
--extra-index-url https://download.pytorch.org/whl/cu124

# Web API
fastapi==0.121.2
uvicorn[standard]==0.38.0
python-multipart==0.0.6

# Validation and settings
pydantic==2.12.3
pydantic[email]==2.12.3
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.44
aiosqlite==0.19.0

# Auth and security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# AI / LLM
langgraph==0.2.76
langgraph-checkpoint==2.1.2
langgraph-checkpoint-postgres>=2.0.0
psycopg[binary,pool]>=3.2.0,<4.0.0
langgraph-prebuilt==0.1.8
langchain-openai>=0.3.0
openai>=1.0.0
chromadb>=0.5.0,<1.0.0

# Core utilities
python-dotenv==1.2.1
pyyaml>=6.0.0
beautifulsoup4>=4.12.0
jinja2>=3.0.0
click>=8.0.0
requests>=2.28.0
chardet>=5.0.0,<6.0.0
charset-normalizer>=3.0.0,<4.0.0
simplejson>=3.19.0
sortedcontainers>=2.4.0
httpx>=0.24.0
aiofiles>=23.0.0
aiohttp>=3.9.0
tqdm>=4.65.0
addict>=2.4.0
datasets>=2.14.0

# Data and indexing
# OCR 2.10 + Python 3.12 会解析到 opencv-python-headless 4.13，要求 numpy>=2。
numpy>=2.0.0
pandas>=2.0.0
pillow>=10.0.0,<11.0.0
joblib>=1.3.0
scikit-learn>=1.3.0

# Vision / OCR / audio
opencv-python==4.8.1.78
paddlepaddle==2.6.2
# 锁定 PaddleOCR 2.x，避免 3.x 与 Paddle 2.6 的接口不兼容（如 set_optimization_level）。
paddleocr>=2.7.0,<3.0.0
modelscope>=1.9.0
funasr>=1.0.0
librosa>=0.10.0,<1.0.0
soundfile>=0.12.0

# PyTorch (CUDA 12.4)
torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124
